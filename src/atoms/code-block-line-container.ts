import { createComponment } from "../util";
import { codeBlockLineContainerStyles } from "../styles";

export const CodeBlockLineContainer = createComponment("div")(
  codeBlockLineContainerStyles
);
